-- [[ Configure LSP ]]
--  This function gets run when an LSP connects to a particular buffer.
local on_attach = function(_, bufnr)
	-- Map for local mode, setting buffer & descr for each use.
end


-- Setup neovim lua configuration
